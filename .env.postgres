# =============================================================================
# POSTGRESQL CONFIGURATION TEMPLATE
# =============================================================================
#
# ИНСТРУКЦИИ:
# 1. Установите PostgreSQL: https://www.postgresql.org/download/
# 2. Создайте базу данных:
#    psql -U postgres -c 'CREATE DATABASE whale_tracker;'
# 3. Скопируйте эти настройки в .env файл
# 4. Запустите: python init_postgres.py
#
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
DB_TYPE=postgresql

# PostgreSQL connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=whale_tracker
DB_USER=postgres
DB_PASSWORD=YOUR_POSTGRES_PASSWORD_HERE

# Connection pool settings
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_ECHO=false  # Set to true for SQL debugging

# =============================================================================
# ПРЕИМУЩЕСТВА POSTGRESQL vs SQLite
# =============================================================================
#
# ✅ PostgreSQL (для production):
#    - Concurrent connections (несколько клиентов одновременно)
#    - Advanced indexing (быстрый поиск по миллионам записей)
#    - Full-text search (поиск по тексту)
#    - JSON queries (запросы к JSON полям)
#    - Transactions (ACID compliance)
#    - Scalability (горизонтальное масштабирование)
#
# ✅ SQLite (для development/testing):
#    - Zero configuration (не нужно устанавливать сервер)
#    - Single file (легко копировать/переносить)
#    - Embedded (работает внутри приложения)
#
# =============================================================================
# КОГДА ИСПОЛЬЗОВАТЬ POSTGRESQL
# =============================================================================
#
# Используйте PostgreSQL если:
# 1. Храните > 100,000 whale detections
# 2. Нужен доступ с нескольких машин
# 3. Требуется high availability
# 4. Планируете analytics/reporting
# 5. Production deployment
#
# Используйте SQLite если:
# 1. Development/testing
# 2. Простой MVP
# 3. Один пользователь
# 4. Не нужна высокая нагрузка
#
# =============================================================================
DB_PASSWORD=Jayaasiri2185 
