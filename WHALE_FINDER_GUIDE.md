# 🐋 Whale Finder - Руководство по использованию

## Описание

`whale_finder.py` - это автоматический инструмент для поиска и анализа качественных криптовалютных "китов" на основе критериев:

1. **Размер капитала** - баланс > $100k
2. **Активность** - регулярные транзакции
3. **Предсказуемость** - паттерны поведения
4. **Качество** - комплексная оценка (0-100)

---

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install aiohttp python-dotenv
```

### 2. Настройка Etherscan API ключа

Получите бесплатный API ключ на https://etherscan.io/apis

Добавьте в `.env` файл:
```env
ETHERSCAN_API_KEY=ваш_ключ_здесь
```

### 3. Базовое использование

```bash
# Анализ примеров адресов (демо режим)
python whale_finder.py

# Анализ конкретных адресов
python whale_finder.py --addresses 0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045,0xAb5801a7D398351b8bE11C439e05C5B3259aeC9B

# Анализ адресов из файла
python whale_finder.py --file example_whale_addresses.txt

# Для конкретного токена
python whale_finder.py --token UNI --token-address 0x1f9840a85d5aF5bf1D1762F925BDADdC4201F984
```

---

## Параметры запуска

### Основные параметры

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--addresses` | Список адресов через запятую | `--addresses 0xABC...,0xDEF...` |
| `--file` | Файл с адресами (по одному на строку) | `--file whales.txt` |
| `--token` | Символ токена (default: ETH) | `--token UNI` |
| `--token-address` | Адрес контракта токена | `--token-address 0x1f9840...` |

### Дополнительные параметры

| Параметр | Описание |
|----------|----------|
| `--no-activity` | Не проверять активность (быстрее) |
| `--export-json` | Экспорт результатов в JSON файл |
| `--export-env` | Показать формат для .env файла |
| `--min-score` | Минимальный score для экспорта (default: 60) |

---

## Примеры использования

### Пример 1: Базовый анализ

```bash
python whale_finder.py --addresses 0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045
```

**Результат:**
```
🔍 Начинаю поиск китов для ETH...
   Адресов для проверки: 1

[1/1] Проверка 0xd8dA6BF26964aF9D7eEd9e03E53415D37aA96045
   💰 Баланс: 245.1234 ETH (~$858,000)
   📊 Анализ активности...
   📈 Активность: 45 tx, 23 исходящих, 5 крупных
   🎯 Паттерн: active_trader
   🔮 Предсказуемость: 65.0/100
   ⭐ Общий score: 72.5/100 (medium confidence)
```

### Пример 2: Анализ из файла с экспортом

```bash
python whale_finder.py --file example_whale_addresses.txt --export-json results.json --export-env
```

### Пример 3: Анализ конкретного токена (UNI)

```bash
python whale_finder.py \
  --token UNI \
  --token-address 0x1f9840a85d5aF5bf1D1762F925BDADdC4201F984 \
  --file uni_whales.txt \
  --export-env \
  --min-score 70
```

---

## Понимание результатов

### Уровни Confidence

- **HIGH (80-100)** ✅ - Отличные киты, рекомендуется мониторить
- **MEDIUM (60-79)** ⚡ - Хорошие киты, можно рассмотреть
- **LOW (40-59)** ⚠️  - Требуют дополнительной проверки
- **VERY_LOW (<40)** ❌ - Не рекомендуется

### Типы паттернов

| Паттерн | Описание | Predictability |
|---------|----------|----------------|
| `foundation_unlock` | Регулярные исходящие на биржи | Очень высокая |
| `vc_fund` | Крупные единоразовые переводы | Высокая |
| `active_trader` | Частые операции на DEX | Средняя |
| `accumulator` | Преимущественно входящие | Низкая |
| `inactive` | Низкая активность | Очень низкая |

### Метрики активности

- **tx_count** - Количество транзакций за последние 30 дней
- **outbound_count** - Количество исходящих транзакций
- **large_moves_count** - Количество крупных переводов (>$50k)
- **unique_destinations** - Количество уникальных адресов назначения

### Overall Score (0-100)

Комплексная оценка качества кита:

- **25%** - Размер капитала
- **25%** - Активность
- **30%** - Предсказуемость
- **20%** - Тип паттерна

---

## Критерии фильтрации

### Автоматически исключаются:

✅ **Черный список:**
- Адреса бирж (Binance, Coinbase, Kraken и т.д.)
- Адреса мостов (Arbitrum, Polygon, Optimism)
- Dead wallets (burn addresses)

✅ **Минимальные пороги:**
- Баланс: $100,000
- Транзакции: 5+ за последние 30 дней
- Исходящие: 2+ транзакции
- Крупные переводы: 1+ (>$50k)

### Настройка критериев

Вы можете изменить пороги в файле `whale_finder.py`:

```python
WHALE_CRITERIA = {
    'min_usd_value': 100000,           # Минимальный баланс
    'min_percentage': 0.1,             # Минимум % от supply
    'recent_activity_days': 30,        # Период активности
    'min_tx_count': 5,                 # Минимум транзакций
    'min_outbound_count': 2,           # Минимум исходящих
    'min_large_moves': 1,              # Минимум крупных
    'large_move_threshold': 50000,     # Порог "крупного"
}
```

---

## Экспорт результатов

### Формат JSON

```bash
python whale_finder.py --file whales.txt --export-json whale_analysis.json
```

**Структура JSON:**
```json
{
  "timestamp": "2025-11-21T12:00:00",
  "total_whales": 5,
  "whales": [
    {
      "address": "0xABC...",
      "token": "ETH",
      "balance_usd": 500000,
      "percentage": 0.15,
      "activity": {
        "tx_count": 25,
        "outbound_count": 12,
        "large_moves": 3,
        "unique_destinations": 5
      },
      "pattern": "foundation_unlock",
      "predictability_score": 80.0,
      "overall_score": 75.5,
      "confidence": "medium"
    }
  ]
}
```

### Формат для .env

```bash
python whale_finder.py --file whales.txt --export-env --min-score 70
```

**Результат:**
```
WHALE_ADDRESSES=0xABC...,0xDEF...,0xGHI...
```

Скопируйте эту строку в ваш `.env` файл!

---

## Практический workflow

### Шаг 1: Сбор адресов

**Источники:**

1. **Etherscan Top Holders**
   ```
   https://etherscan.io/token/TOKEN_ADDRESS#balances
   ```
   - Копируйте адреса из топ-100 holders
   - Исключайте биржи (будет автоматически)

2. **Dune Analytics**
   ```
   https://dune.com/
   ```
   - Ищите дашборды по конкретным токенам
   - Экспортируйте CSV с адресами

3. **Whale Alert Twitter**
   ```
   https://twitter.com/whale_alert
   ```
   - Записывайте адреса из недавних алертов

### Шаг 2: Создание файла с адресами

Создайте `my_whales.txt`:
```
0xAddress1
0xAddress2
0xAddress3
# И т.д.
```

### Шаг 3: Запуск анализа

```bash
python whale_finder.py --file my_whales.txt --export-json results.json --export-env
```

### Шаг 4: Анализ результатов

1. Откройте `results.json` для детального анализа
2. Проверьте киты с **HIGH confidence**
3. Изучите паттерны поведения

### Шаг 5: Добавление в мониторинг

Скопируйте строку `WHALE_ADDRESSES=...` в ваш `.env` файл.

---

## Важные ограничения

### ⚠️  Etherscan API

**Проблема:** Etherscan API не предоставляет прямой endpoint для получения списка holders токена.

**Решения:**

1. **Вручную:** Скопировать адреса с Etherscan UI
2. **Dune Analytics:** Использовать SQL запросы
3. **The Graph:** GraphQL запросы к subgraph
4. **Nansen/Arkham:** Платные аналитические платформы

### ⚠️  Price API

Скрипт использует **примерную** оценку цены ETH ($3,500).

Для точного расчета в USD интегрируйте:
- CoinGecko API
- CoinMarketCap API
- DEXScreener API

### ⚠️  Rate Limits

Etherscan free tier: **5 calls/second**

При анализе большого количества адресов добавьте задержки:
```python
await asyncio.sleep(0.2)  # 5 calls/sec
```

---

## Расширение функционала

### Добавление новых источников данных

```python
# В класс EtherscanClient добавьте:

async def get_token_holders_from_dune(self, token_address: str):
    """Получить holders через Dune Analytics API"""
    # Ваша реализация
    pass
```

### Добавление новых паттернов

```python
# В метод detect_pattern добавьте:

if self.is_wash_trader(activity):
    return 'wash_trader'
```

### Backtest на исторических данных

```python
async def backtest_whale(self, whale_address: str, months: int = 6):
    """Проверка точности предсказаний на исторических данных"""
    # Получить транзакции за последние N месяцев
    # Для каждого перевода на биржу проверить изменение цены
    # Рассчитать accuracy
    pass
```

---

## Troubleshooting

### Ошибка: "Etherscan API error: Max rate limit reached"

**Решение:** Подождите 1 секунду или используйте платный API key.

### Ошибка: "Timeout при запросе к Etherscan API"

**Решение:** Увеличьте timeout в коде:
```python
async with session.get(url, timeout=60) as response:
```

### Предупреждение: "Etherscan API не предоставляет прямой доступ к holders"

**Решение:** Используйте альтернативные источники (см. "Важные ограничения").

---

## Best Practices

### ✅ Рекомендуется

- Начинайте с 5-10 адресов для теста
- Используйте `--export-json` для сохранения результатов
- Регулярно обновляйте список китов (раз в неделю)
- Проверяйте киты с HIGH confidence в первую очередь

### ❌ Не рекомендуется

- Анализировать >100 адресов за раз (rate limits)
- Полагаться только на overall_score (проверяйте паттерн)
- Игнорировать киты с MEDIUM confidence (могут быть ценные)
- Использовать без Etherscan API ключа (быстро достигнете лимитов)

---

## FAQ

**Q: Сколько китов нужно мониторить?**
A: Начните с 5-10 качественных (HIGH confidence). Постепенно расширяйте до 20-30.

**Q: Какой паттерн самый надежный?**
A: `foundation_unlock` - самая высокая предсказуемость.

**Q: Можно ли использовать для других сетей (BSC, Polygon)?**
A: Да, но нужно изменить `base_url` в EtherscanClient на соответствующий API (bscscan.com, polygonscan.com).

**Q: Как часто запускать анализ?**
A: Раз в неделю для обновления списка, раз в месяц для полного пересмотра.

**Q: Что делать если все киты отфильтрованы?**
A: Понизьте пороги в `WHALE_CRITERIA` или проверьте правильность адресов.

---

## Дополнительные ресурсы

- **Etherscan API Docs:** https://docs.etherscan.io/
- **Whale Alert:** https://whale-alert.io/
- **Dune Analytics:** https://dune.com/
- **Nansen:** https://nansen.ai/
- **Arkham Intelligence:** https://platform.arkhamintelligence.com/

---

**Версия:** 1.0
**Дата:** 2025-11-21
**Автор:** Whale Tracker Project
