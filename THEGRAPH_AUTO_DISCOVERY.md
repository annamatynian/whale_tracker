# 🤖 Автоматический поиск китов через The Graph

## Обзор

Вместо ручного копирования адресов, система **автоматически** находит крупных holders через The Graph subgraphs, затем анализирует их качество.

### Преимущества автоматизации:

✅ **Быстро** - один запрос вместо ручного копирования
✅ **Точно** - актуальные данные напрямую из блокчейна
✅ **Масштабируемо** - легко анализировать любые токены
✅ **Фильтрация** - автоматическое исключение бирж и мелких holders

---

## Быстрый старт

### 1. Получите The Graph API key (опционально)

The Graph бесплатен, но для production рекомендуется API key:

1. Откройте https://thegraph.com/studio/
2. Подключите кошелек
3. Создайте API key
4. Добавьте в `.env`:
   ```env
   THEGRAPH_API_KEY=ваш_ключ_здесь
   ```

### 2. Запустите автопоиск

**Для известных токенов (пресеты):**
```bash
python whale_finder_auto.py --preset UNI --limit 50
```

**Для любых токенов:**
```bash
python whale_finder_auto.py \
  --token-address 0x1f9840a85d5aF5bf1D1762F925BDADdC4201F984 \
  --subgraph-id EYCKATKGBKLWvSfwvBjzfCBmGwYNdVkduYXVivCsLRFu \
  --token-symbol UNI \
  --limit 50
```

---

## Примеры использования

### Пример 1: Поиск UNI китов

```bash
python whale_finder_auto.py --preset UNI --limit 20 --export-env
```

**Результат:**
```
🤖 АВТОМАТИЧЕСКИЙ ПОИСК КИТОВ ДЛЯ UNI
================================================================================

📡 ШАГ 1: Получение holders через The Graph
   Token: 0x1f9840a85d5aF5bf1D1762F925BDADdC4201F984
   Subgraph: EYCKATKGBKLWvSfwvBjzfCBmGwYNdVkduYXVivCsLRFu
   Минимальный баланс: $100,000
   Лимит: 20 holders

✅ Получено 20 holders

Топ-5 крупнейших holders:
   1. 0xd8dA6BF2...aA96045
      Balance: 250,000.00 tokens (0.25%)
   ...

🔍 ШАГ 2: Анализ качества китов
   Проверка активности: ДА

[Детальный анализ каждого кита...]

✅ ШАГ 3: Анализ завершен
   Проанализировано: 20 адресов
   Прошли фильтры: 12 китов

[Результаты анализа]

================================================================================
📋 ФОРМАТ ДЛЯ .ENV ФАЙЛА
================================================================================

WHALE_ADDRESSES=0xABC...,0xDEF...,0xGHI...

(12 адресов с score >= 60)
```

### Пример 2: Поиск без проверки активности (быстро)

```bash
python whale_finder_auto.py --preset LINK --limit 100 --no-activity
```

Получает 100 holders **без** проверки активности через Etherscan.
**Быстро**, но менее точно.

### Пример 3: Экспорт в JSON

```bash
python whale_finder_auto.py \
  --preset AAVE \
  --limit 50 \
  --export-json aave_whales.json \
  --export-env \
  --min-score 70
```

Сохранит результаты в `aave_whales.json` и покажет формат для `.env`.

---

## Доступные пресеты

| Токен | Адрес | Описание |
|-------|-------|----------|
| `UNI` | 0x1f9840... | Uniswap token |
| `LINK` | 0x514910... | Chainlink token |
| `AAVE` | 0x7Fc665... | Aave token |

**Добавление новых пресетов:**

Отредактируйте `whale_finder_auto.py`, добавьте в `TOKEN_PRESETS`:

```python
TOKEN_PRESETS = {
    'YOUR_TOKEN': {
        'name': 'Your Token Name',
        'token_address': '0x...',
        'subgraph_id': '...',
        'decimals': 18
    },
}
```

---

## Как найти Subgraph ID

### Метод 1: The Graph Explorer

1. Откройте https://thegraph.com/explorer
2. Поиск: `"token name" holders` или `"token name" balances`
3. Выберите subgraph с entity `tokenHolders` или `accounts`
4. Скопируйте Subgraph ID из URL

### Метод 2: Популярные subgraphs

**Uniswap V2:**
```
Subgraph ID: 5zvR82QoaXYFyDEKLZ9t6v9adgnptxYpKpSbxtgVENFV
Entity: pairs (не holders напрямую)
```

**Uniswap V3:**
```
Subgraph ID: EYCKATKGBKLWvSfwvBjzfCBmGwYNdVkduYXVivCsLRFu
Entity: positions (holders через positions)
```

**Generic ERC20 (если есть):**
Зависит от токена - не все токены имеют dedicated subgraphs.

### Метод 3: Создать свой subgraph

Если для токена нет subgraph с holders:

1. Создайте subgraph на https://thegraph.com/studio/
2. Индексируйте `Transfer` events контракта
3. Создайте entity `TokenHolder` с балансами

---

## Параметры запуска

### Обязательные параметры (один из):

| Параметр | Описание | Пример |
|----------|----------|--------|
| `--preset TOKEN` | Использовать пресет | `--preset UNI` |
| `--token-address ADDR` | Адрес контракта (требует --subgraph-id) | `--token-address 0x1f9840...` |

### Дополнительные параметры:

| Параметр | Описание | Default | Пример |
|----------|----------|---------|--------|
| `--subgraph-id ID` | The Graph subgraph ID | - | `--subgraph-id EYC...` |
| `--token-symbol SYM` | Символ для отображения | TOKEN | `--token-symbol UNI` |
| `--min-balance USD` | Минимальный баланс в USD | 100000 | `--min-balance 250000` |
| `--limit N` | Количество holders | 50 | `--limit 100` |
| `--no-activity` | Не проверять активность | false | `--no-activity` |
| `--export-json FILE` | Экспорт в JSON | - | `--export-json results.json` |
| `--export-env` | Показать формат .env | false | `--export-env` |
| `--min-score N` | Минимальный score для экспорта | 60 | `--min-score 70` |

---

## Архитектура системы

```
┌─────────────────────────────────────────────────────────┐
│          whale_finder_auto.py (CLI)                      │
│         AutoWhaleFinderIntegrated                        │
└────────┬────────────────────────────────────────┬────────┘
         │                                        │
         ▼                                        ▼
┌──────────────────────┐              ┌────────────────────┐
│ TheGraphHoldersClient│              │   WhaleFinder      │
│  (thegraph_holders_  │              │  (whale_finder.py) │
│   client.py)         │              │                    │
│                      │              │                    │
│ • GraphQL queries    │              │ • Activity check   │
│ • Holder parsing     │              │ • Pattern detect   │
│ • Balance calc       │              │ • Quality scoring  │
└──────────┬───────────┘              └──────────┬─────────┘
           │                                     │
           ▼                                     ▼
    ┌──────────────┐                    ┌────────────────┐
    │  The Graph   │                    │   Etherscan    │
    │  Subgraphs   │                    │      API       │
    └──────────────┘                    └────────────────┘
```

**Процесс:**

1. **Шаг 1:** The Graph → Получение списка holders (автоматически)
2. **Шаг 2:** Etherscan → Проверка активности каждого holder
3. **Шаг 3:** Scoring → Анализ качества и паттернов
4. **Шаг 4:** Export → Формат для .env или JSON

---

## Troubleshooting

### Проблема: "No holders found for token"

**Причина:** Subgraph не содержит entity `tokenHolders` или `accounts`

**Решение:**
1. Проверьте schema subgraph на The Graph Explorer
2. Попробуйте другой subgraph для этого токена
3. Используйте ручной режим (`whale_finder.py --file ...`)

### Проблема: "GraphQL errors: ..."

**Причина:** Неверный GraphQL query или subgraph недоступен

**Решение:**
1. Проверьте subgraph_id
2. Проверьте что subgraph активен (не deprecated)
3. Попробуйте другой subgraph

### Проблема: "Timeout querying subgraph"

**Причина:** Subgraph перегружен или медленный

**Решение:**
1. Уменьшите `--limit`
2. Попробуйте позже
3. Используйте другой subgraph

### Проблема: "All whales filtered out"

**Причина:** Все holders не прошли фильтры качества

**Решение:**
1. Понизьте `--min-balance`
2. Используйте `--no-activity` (пропустит проверку активности)
3. Увеличьте `--limit` для получения большего количества holders

---

## Сравнение: Авто vs Ручной

| Критерий | Автоматический (The Graph) | Ручной (Etherscan UI) |
|----------|----------------------------|------------------------|
| **Скорость** | ⚡ Быстро (1 запрос) | 🐌 Медленно (копировать вручную) |
| **Точность** | ✅ Актуальные данные | ⚠️  Может устареть |
| **Масштаб** | ✅ Легко 100+ holders | ❌ Сложно >20 |
| **Фильтрация** | ✅ Автоматическая | ❌ Вручную |
| **Setup** | ⚠️  Нужен subgraph ID | ✅ Не нужен |
| **Доступность** | ⚠️  Не для всех токенов | ✅ Для всех |

**Рекомендация:**

- **Используйте AUTO** если есть subgraph для вашего токена
- **Используйте РУЧНОЙ** если subgraph недоступен

---

## Интеграция с основной системой

После получения адресов через AUTO:

```bash
# 1. Автопоиск с экспортом
python whale_finder_auto.py --preset UNI --export-env --min-score 70

# 2. Скопируйте строку WHALE_ADDRESSES=... в .env

# 3. Запустите мониторинг
python main.py
```

---

## Расширение: Добавление новых токенов

### Вариант 1: Добавить пресет (рекомендуется)

Отредактируйте `whale_finder_auto.py`:

```python
TOKEN_PRESETS = {
    'YOUR_TOKEN': {
        'name': 'Your Token Name',
        'token_address': '0xYourTokenAddress',
        'subgraph_id': 'YourSubgraphID',  # Найти на thegraph.com
        'decimals': 18  # Из контракта токена
    },
}
```

### Вариант 2: Использовать напрямую

```bash
python whale_finder_auto.py \
  --token-address 0xYourTokenAddress \
  --subgraph-id YourSubgraphID \
  --token-symbol YOUR_TOKEN
```

---

## FAQ

**Q: Нужен ли The Graph API key?**
A: Опционально. Без key работает, но с лимитами. Для production рекомендуется.

**Q: Какие токены поддерживаются?**
A: Любые, для которых есть subgraph с entity `tokenHolders` или `accounts`.

**Q: Можно ли использовать для BSC/Polygon?**
A: Да! Найдите соответствующий subgraph на The Graph для нужной сети.

**Q: Сколько holders можно получить?**
A: The Graph обычно лимит 1000 за запрос. Можно использовать pagination.

**Q: Что если subgraph не содержит holders?**
A: Используйте ручной режим (`whale_finder.py`) с адресами из Etherscan UI.

**Q: Как часто обновлять список китов?**
A: Раз в неделю достаточно для большинства случаев.

---

## Дополнительные ресурсы

- **The Graph Explorer:** https://thegraph.com/explorer
- **The Graph Docs:** https://thegraph.com/docs
- **Создание Subgraphs:** https://thegraph.com/docs/en/developing/creating-a-subgraph/
- **Etherscan API:** https://docs.etherscan.io/

---

**Версия:** 1.0
**Дата:** 2025-11-21
**Автор:** Whale Tracker Project
