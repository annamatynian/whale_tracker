# 🧪 Руководство по Тестированию Системы

## 🚀 Быстрый Старт (5 минут)

```bash
# Перейдите в корень проекта
cd C:\Users\annam\Documents\DeFi-RAG-Project\crypto-multi-agent-system

# Запустите мастер-тест (все тесты сразу)
python test_master_suite.py
```

## 📋 Пошаговое Тестирование

### ШАГ 1: Базовая Проверка
```bash
# Проверка импортов и основных компонентов
python test_imports_only.py
python test_quick.py
```

### ШАГ 2: Компонентные Тесты
```bash
# Тест архитектуры с mock данными
python test_mock_data.py

# Тест системы скоринга на реальных примерах
python test_scoring_examples.py

# Тест архитектуры многоуровневой воронки
python test_funnel_architecture.py
```

### ШАГ 3: API Интеграции (требует .env)
```bash
# Тест внешних API (нужны ключи)
python test_api_integrations.py
```

### ШАГ 4: E2E Тестирование
```bash
# Полный тест системы от начала до конца
python test_full_e2e_pipeline.py
```

## 🎯 Что Ожидать от Тестов

### ✅ Успешные Результаты
- Все импорты работают без ошибок
- Mock данные корректно обрабатываются
- Система скоринга правильно оценивает примеры из PDF
- Многоуровневая воронка корректно фильтрует кандидатов
- API интеграции функционируют (с правильными ключами)

### ⚠️ Ожидаемые Проблемы (Нормально)
- API ошибки без .env файла
- Ограничения бесплатных API лимитов
- Отсутствие реальных токенов для анализа в тестовом режиме

### ❌ Критические Проблемы (Требуют Исправления)
- ImportError - проблемы с зависимостями
- Ошибки инициализации основных компонентов
- Провал mock тестов
- Неправильная работа системы скоринга

## 🔧 Troubleshooting

### Проблема: ImportError
```bash
# Проверьте что вы в правильной директории
pwd
ls agents/  # должна существовать

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: API Ошибки
```bash
# Проверьте .env файл
ls -la .env

# Скопируйте пример
cp .env.example .env
# Отредактируйте .env своими ключами
```

### Проблема: Telegram Ошибки
- Создайте Telegram Bot через @BotFather
- Добавьте TELEGRAM_BOT_TOKEN в .env
- Добавьте TELEGRAM_CHAT_ID в .env

## 📊 Интерпретация Результатов

### 🎯 100% Успех = Система Готова
- Все критические компоненты работают
- Можно переходить к настройке продакшена
- Добавьте API ключи и запускайте

### ⚡ 80-99% Успех = В Основном Готова  
- Критическая функциональность работает
- Есть минорные проблемы с API интеграциями
- Исправьте найденные проблемы

### 🔧 50-79% Успех = Требует Доработки
- Базовая архитектура работает
- Есть серьезные проблемы с компонентами
- Необходима отладка

### 💥 <50% Успех = Критические Проблемы
- Система не готова к использованию
- Множественные ошибки в ключевых компонентах
- Требуется серьезная отладка

## 🎯 После Успешного Тестирования

1. **Настройте .env файл:**
   ```env
   DEXSCREENER_API_KEY=your_key_here
   COINGECKO_API_KEY=your_key_here
   GOPLUS_API_KEY=your_key_here
   TELEGRAM_BOT_TOKEN=your_bot_token
   TELEGRAM_CHAT_ID=your_chat_id
   ```

2. **Запустите проверку конфигурации:**
   ```bash
   python main.py --config-check
   ```

3. **Запустите систему в тестовом режиме:**
   ```bash
   python main.py --dry-run
   ```

4. **Запустите полную систему:**
   ```bash
   python main.py
   ```

## 📋 Созданные Тестовые Файлы

| Файл | Назначение |
|------|-----------|
| `test_master_suite.py` | 🎯 Мастер-тест (запуск всех тестов) |
| `test_api_integrations.py` | 🔗 Тест API подключений |
| `test_funnel_architecture.py` | 🔍 Тест многоуровневой воронки |
| `test_full_e2e_pipeline.py` | 🚀 E2E тест всей системы |

---

💡 **Совет**: Начните с `test_master_suite.py` - он запустит все тесты в правильной последовательности и даст полный отчет о готовности системы.
