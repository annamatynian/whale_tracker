# 🚀 Quick Start Guide - LP Health Tracker

## Пошаговая инструкция для первого запуска

### Шаг 1: Подготовка окружения
```bash
# Перейдите в папку проекта
cd lp_health_tracker

# Установите зависимости
pip install -r requirements.txt
```

### Шаг 2: Настройка API ключей

1. **Получите RPC ключ** (выберите один):
   - [Infura](https://app.infura.io/) - создайте проект, скопируйте Project ID
   - [Alchemy](https://dashboard.alchemy.com/) - создайте приложение, скопируйте API Key
   - Или используйте бесплатные Ankr endpoints

2. **Настройте Telegram бота**:
   - Отправьте `/newbot` боту [@BotFather](https://t.me/BotFather)
   - Выберите имя и получите TOKEN
   - Отправьте `/start` боту [@userinfobot](https://t.me/userinfobot) для получения Chat ID

### Шаг 3: Создание .env файла
```bash
# Скопируйте пример
cp .env.example .env

# Отредактируйте .env файл:
nano .env
```

**Минимально необходимые настройки:**
```env
# Один из RPC провайдеров
INFURA_API_KEY="your_infura_project_id"
# ИЛИ
ALCHEMY_API_KEY="your_alchemy_key"

# Telegram настройки  
TELEGRAM_BOT_TOKEN="123456789:ABCdefGHIjklmnop"
TELEGRAM_CHAT_ID="987654321"

# Ваш кошелек
WALLET_ADDRESSES="0xYourWalletAddress"
```

### Шаг 4: Добавление позиций

**Вариант A: Интерактивно**
```bash
python run.py --add-position
```

**Вариант B: Из примера**
```bash
# Скопируйте пример
cp data/positions.json.example data/positions.json

# Отредактируйте своими данными
nano data/positions.json
```

### Шаг 5: Тестирование
```bash
# Проверьте конфигурацию
python run.py --test-config

# Проверьте позиции
python run.py --list-positions
```

### Шаг 6: Запуск!
```bash
# Запустите агент
python run.py
```

## 🎯 Готовые конфигурации

### Для тестирования (Sepolia):
```env
DEFAULT_NETWORK="ethereum_sepolia"
WALLET_ADDRESSES="0xYourTestWalletAddress"
CHECK_INTERVAL_MINUTES=5
```

### Для продакшена (Mainnet):
```env
DEFAULT_NETWORK="ethereum_mainnet"
CHECK_INTERVAL_MINUTES=15
DEFAULT_IL_THRESHOLD=0.05
```

## 🆘 Если что-то не работает

### Проблема: "Configuration errors"
```bash
python run.py --test-config
# Исправьте ошибки в .env файле
```

### Проблема: "No positions found"
```bash
# Добавьте позицию
python run.py --add-position
```

### Проблема: "Telegram not responding"
- Проверьте токен бота
- Убедитесь, что отправили `/start` вашему боту
- Проверьте Chat ID

### Проблема: "Web3 connection failed"
- Проверьте API ключ RPC провайдера
- Попробуйте другой провайдер (Infura → Alchemy)

## 📋 Checklist первого запуска

- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Создан .env файл из .env.example
- [ ] Получен и настроен RPC API ключ
- [ ] Создан Telegram бот и получен токен
- [ ] Получен Telegram Chat ID
- [ ] Добавлена хотя бы одна LP позиция
- [ ] Тест конфигурации прошел успешно (`--test-config`)
- [ ] Агент запущен и отправил стартовое уведомление

## 🎉 Поздравляем!

Если все шаги выполнены успешно, вы должны увидеть:

1. ✅ Успешное подключение к сети
2. ✅ Тестовое Telegram сообщение
3. ✅ Стартовое уведомление от бота
4. 🔄 Регулярные проверки каждые N минут

**Агент теперь мониторит ваши LP позиции и будет уведомлять о значительных изменениях IL!**

---

📖 **Подробная документация**: см. [README.md](README.md)  
🐛 **Проблемы**: создайте Issue в репозитории  
💬 **Вопросы**: обсуждения в Discussions
