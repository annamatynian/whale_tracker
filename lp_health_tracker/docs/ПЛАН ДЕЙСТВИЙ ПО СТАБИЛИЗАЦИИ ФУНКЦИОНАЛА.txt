План действий по стабилизации функционала
Вот конкретный пошаговый план, чтобы убедиться, что система стабильно работает с текущими статическими порогами.

Шаг 1: Реализация системы уведомлений (Срок: 2 дня)
Создание модуля: Разработать новый файл src/notification_manager.py с классом NotificationManager.

Метод отправки: Внутри этого класса создать метод send_telegram_message(message), который будет использовать TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID из settings.py для отправки сообщений через API Telegram.

Шаг 2: Интеграция проверки порогов в главный цикл (Срок: 1 день)
Проверка порогов: В simple_multi_pool.py, после того как NetPnLCalculator провел полный анализ позиции, вызвать метод check_alert_thresholds (из data_analyzer.py).

Логика оповещения: Если check_alert_thresholds возвращает, что порог IL превышен (il_threshold_crossed: True), SimpleMultiPoolManager должен:

Сформировать понятное сообщение для пользователя (например, "🔴 ВНИМАНИЕ: IL по WETH-USDC достиг -5.72%, что выше вашего порога в -5%").

Вызвать notification_manager.send_telegram_message() для отправки этого сообщения.

Шаг 3: Комплексное тестирование (End-to-End) (Срок: 2 дня)
Создание тестового сценария: В MockDataProvider создать специальный сценарий, например "high_il_alert_scenario", который будет генерировать цены, гарантированно приводящие к IL выше порога в 5%.

Тест на срабатывание: Запустить главный скрипт с этим сценарием и убедиться, что реальное сообщение приходит в Telegram.

Тест на "молчание": Запустить скрипт со стандартным сценарием (где IL ниже порога) и убедиться, что никаких лишних сообщений не приходит.

Тест на отказ: Сымитировать ошибку отправки в Telegram и проверить, что программа не "падает", а корректно логирует ошибку.

Шаг 4: Мониторинг в "живом" режиме (Срок: ~1 неделя)
Запуск с LiveDataProvider: После успешного прохождения всех тестов запустить агент в рабочем режиме с LiveDataProvider.

Наблюдение: Наблюдать за его работой в течение нескольких дней, анализируя логи и получаемые уведомления. Это позволит убедиться, что система стабильно работает с непредсказуемыми реальными данными.

После выполнения этих четырех шагов мы можем быть на 100% уверены, что базовый функционал стабилен и готов к дальнейшим усложнениям.