# Совместимые зависимости без жестких версий (используем то что есть)
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# Для основного запуска
APScheduler>=3.10.0
aiohttp>=3.9.0
requests>=2.31.0

# Для логирования  
colorlog>=6.8.0
